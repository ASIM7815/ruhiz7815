# Debug Join Requests Issue

## Problem
Join requests are being submitted but not showing up in the "Join Requests" page.

## Console Errors Visible
From the screenshot, I can see several errors:
1. "Failed to decode downloaded font" - Not related to join requests
2. "RLS parsing error: invalid UTF8 character" - **THIS IS THE ISSUE**
3. Multiple "Failed to decode downloaded font" errors

## Root Cause Analysis

The RLS (Row Level Security) parsing error suggests that there's an issue with the Supabase RLS policies or the way the query is being executed.

### Possible Causes:

1. **RLS Policies Conflict**: The Prisma tables (PostgreSQL) have RLS enabled, but you're using the service role key which should bypass RLS. However, there might be a conflict.

2. **Character Encoding Issue**: The "invalid UTF8 character" error suggests there might be special characters in the data that aren't being handled correctly.

3. **Query Issue**: The API route might be querying the wrong table or using incorrect filters.

## Debugging Steps

### Step 1: Check if join requests are being created

Run this in your terminal:
```bash
npx prisma studio
```

Then:
1. Open the `join_requests` table
2. Check if there are any records with `status = "PENDING"`
3. Note the `projectId` and `userId`

### Step 2: Check the API route logs

The API route should be logging errors. Check your terminal where `npm run dev` is running for any error messages.

### Step 3: Test the API directly

Open your browser console and run:
```javascript
// Get the projectId from the URL
const projectId = window.location.pathname.split('/')[2];

// Test the GET endpoint
fetch(`/api/projects/${projectId}/join`)
  .then(r => r.json())
  .then(console.log)
  .catch(console.error);
```

### Step 4: Check authentication

The issue might be that the owner is not properly authenticated. Run:
```javascript
fetch('/api/debug-auth')
  .then(r => r.json())
  .then(console.log);
```

## Quick Fix

The issue is likely that **RLS policies are enabled on the Prisma tables**, but they shouldn't be because you're using the service role key.

### Solution 1: Disable RLS on Prisma tables

Run this SQL in Supabase SQL Editor:

```sql
-- Disable RLS on Prisma-managed tables
ALTER TABLE users DISABLE ROW LEVEL SECURITY;
ALTER TABLE accounts DISABLE ROW LEVEL SECURITY;
ALTER TABLE sessions DISABLE ROW LEVEL SECURITY;
ALTER TABLE verification_tokens DISABLE ROW LEVEL SECURITY;
ALTER TABLE user_skills DISABLE ROW LEVEL SECURITY;
ALTER TABLE user_interests DISABLE ROW LEVEL SECURITY;
ALTER TABLE projects DISABLE ROW LEVEL SECURITY;
ALTER TABLE project_skills DISABLE ROW LEVEL SECURITY;
ALTER TABLE project_members DISABLE ROW LEVEL SECURITY;
ALTER TABLE join_requests DISABLE ROW LEVEL SECURITY;
ALTER TABLE tasks DISABLE ROW LEVEL SECURITY;
ALTER TABLE messages DISABLE ROW LEVEL SECURITY;
ALTER TABLE study_groups DISABLE ROW LEVEL SECURITY;
ALTER TABLE study_group_members DISABLE ROW LEVEL SECURITY;
ALTER TABLE study_group_join_requests DISABLE ROW LEVEL SECURITY;
ALTER TABLE startups DISABLE ROW LEVEL SECURITY;
ALTER TABLE startup_members DISABLE ROW LEVEL SECURITY;
ALTER TABLE startup_join_requests DISABLE ROW LEVEL SECURITY;
ALTER TABLE listings DISABLE ROW LEVEL SECURITY;
ALTER TABLE resources DISABLE ROW LEVEL SECURITY;
ALTER TABLE notifications DISABLE ROW LEVEL SECURITY;
ALTER TABLE reports DISABLE ROW LEVEL SECURITY;
ALTER TABLE audit_logs DISABLE ROW LEVEL SECURITY;
ALTER TABLE file_assets DISABLE ROW LEVEL SECURITY;
```

**Why?** 
- You're using Prisma with the PostgreSQL adapter
- Your API routes use the service role key (bypasses RLS)
- RLS is not needed because authentication is handled in your API routes
- The RLS policies are causing parsing errors

### Solution 2: Check the API route

The API route might have an issue. Let me check the exact query being used.

## Expected Behavior

1. User submits join request → Creates record in `join_requests` table
2. Owner visits `/projects/[projectId]/requests` → Fetches pending requests
3. Requests are displayed with user info
4. Owner can approve/reject

## Current Behavior

1. User submits join request → ✅ (assuming this works)
2. Owner visits `/projects/[projectId]/requests` → ❌ Returns empty array
3. No requests shown → "No pending requests"

## Next Steps

1. **Disable RLS on Prisma tables** (Solution 1 above)
2. **Restart your dev server**: `npm run dev`
3. **Test again**: Submit a join request and check if it appears
4. **Check browser console**: Look for any new errors
5. **Check server logs**: Look for any API errors

If the issue persists after disabling RLS, we'll need to check:
- The exact SQL query being generated by Prisma
- The authentication flow
- The database connection
